### Changed
- **`read_file` が「見つかりません」と言うとき、その場所に実在する名前を併せて返す**。`agent-implement-0.log` のように
  初段の名前を外したときでも、応答に「その run にあるのは 3 件: agent-plan-0.log, state.json, work/」が付くので、
  `run_show` を引き直さずに次の一手が分かります。run 自体が無いときは「ディレクトリが見つかりません」と言い分けます。
  返すのは名前だけ（中身・サイズ・時刻は載せません。`.` で始まる名前は出さず、多いときは先頭 40 件と残りの件数）。
  MCP の `ticket_attach(path=...)` が ctl 上のファイルを見つけられなかったときも同じ形です。

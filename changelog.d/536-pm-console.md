### Added
- **管理役（PM）の様子を見る画面**。console の左ナビに「管理役」（`#/pm`、`g p`）が増え、いまの状態・次に回す
  チケットとその理由・判断の記録（新しい順に、日時・対象・決めたこと・**理由**）・板の要約が 1 枚に出ます
  （`GET /api/pm` を 5 秒ごとに読むだけで、何も起動しません。ADR-0074）。**板をまだ読めていないときは
  「回すチケットがありません」とは言わず「状態を取得できていません」**と出るので、0 件と取得できていない状態が
  画面でも混ざりません。置き場を作ったばかりの console は正常にこの状態になるため、危険色にはしていません。
  「モードを切り替える」「一時停止する」「今すぐ回す」は置き場だけで、つながっていないことを画面に出しています。

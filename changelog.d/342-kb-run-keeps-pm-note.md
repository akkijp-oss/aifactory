### Fixed
- **kb: `kb run` / `kb sync` / 再走が、人が書いたチケットのメモを上書きして消さなくなった**。run 由来の状態文はメモの 1 行目
  `[run] …` に置き、2 行目以降（`kb set --note` で人が書いた申し送り）はそのまま残す。人がメモを書くときは今までどおり
  全体を書き替える。`[run] ` が付く前の run 由来の文言で始まる既存のメモは次の run で捨てるので、データの移行は要らない（ADR-0048）

### Added
- **コンソール: 設定のトップから既定モデル（`routes.env` の 4 行）を直接変えられる**。これまで経路表を変えるには、そのクラスを使う工程を探して工程の詳細まで降りる必要があり、どの工程からも参照されない `MODEL_default` は画面から変えられなかった。設定のトップの「モデルの経路」に 4 行それぞれの入力欄と「変更を確かめる」が出る。下見（変更前後の実効値・影響する全工程・動いている run）・確認・版を見た保存という流れは工程から変えるときと同じ（ADR-0065）。

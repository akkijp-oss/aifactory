### Added
- **Mac worker: 止まったゲストを起動し直す `guest-start`。`kb run <id> --resume` が自動で呼ぶ**。今まではゲストが止まると続きを回す手段が無く、`guest-release` → lease 解放 → 新規 run で実装をやり直すしかなかった。`--resume` は lease を確かめた直後にゲストを起動し直し、そこから従来どおり工程履歴の続きに進む。ゲストが一覧に無い・起動できないときは黙って作り直さずに失敗し、lease もゲストも人の検査のために残す（ADR-0068）。

### Fixed
- **console: 再走で `-attemptN` へ退避された run が永久に「実行中」と出ていたのを直した**。退避された run はどのジョブとも名前が一致しない（起動したジョブの `run_hint` は退避前の名前のまま）ため、生死の判定が 3 経路とも外れて死んだ記録が実行中のまま残っていた。退避前の名前（兄弟 run）を指すジョブと、完了（`done`）した票の更新時刻も根拠として読むようにした。根拠の無い run を勝手に中断にしない約束は変えていない。

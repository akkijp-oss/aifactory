### Changed
- **console: チケット詳細を「読む → 状況 → 操作 → 記録」の順に並べ替えた**。見出しとメモの次に、動いていれば実行状況の 1 行（今の工程と経過時間、実行記録へのリンク）が出て、その次に本文（末尾の「## 完了条件」を含む）と添付が来る。実行・状態を進める・項目を直すは「操作」1 枚にまとまり、その後（広い画面では右）に置かれる。幅の狭い画面と読み上げでも同じ順で読める（ADR-0056）。
- **console: 実行の詳細設定を畳んだ**。workflow の選び直し・`--keep`・`--resume`・dry-run は「実行の詳細設定」を開くと出る。「実行する」と状態別の案内は畳まない。
- **console: 操作領域の強いボタンを「実行する」だけにした**。状態を進めるボタンは強い色をやめ、「runner は動かしません。記録の状態だけを変えます。」を添える。完了ずみのチケットで「未着手に戻す（やり直す）」が実行欄と状態欄に 2 個出ていたのをやめ、いつでもちょうど 1 個にした（実行欄が警告に差し替わる PJ に project.yml が無いとき・ジョブが動いているときは、状態欄に残す）。

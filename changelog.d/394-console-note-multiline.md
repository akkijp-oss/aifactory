### Fixed
- **console: チケット詳細の「項目を直す」のメモが複数行欄になり、改行を失わなくなった**。これまでは 1 行の入力欄
  （`input type=text`）だったため、複数行の申し送りを開くと行が連結されて読めず、**何も編集せずに「保存する」を押しただけで
  記録側の改行まで消えていた**。欄を `textarea` にし、保存は入力を無変換で送る（空白だけの入力を「メモを空にする」として
  扱う従来の動きは変わらない）。読み取り専用のメモ枠も改行のまま表示する（ADR-0061）。

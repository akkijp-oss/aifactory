### Fixed
- **画面操作: Linuxの`CMD`/`WIN`は文字入力を抑えないことを明記した**。`CMD`+`SHIFT`+`=` はLinux（Super）ではショートカットにならず `+` が入る。Linuxのショートカットは `CTRL` か `ALT` で送る。あわせて、この挙動を読み落として3回に2回落ちていたX11の画面操作テストを、入力の反映を待ってから比べる形に直した。

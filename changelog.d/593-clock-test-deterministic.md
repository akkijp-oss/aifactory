### Fixed
- **test_sandbox_clock: ゲストの時計ずれのテストが実行した時刻で落ちなくなった**。偽ゲストと、切り出した `sync_clock` 本体が別々に本物の `date -u +%s` を呼んでいたため、その 2 回の間で秒が跨いだ回だけ測定値が 1 秒ずれ、`578400s` を期待する断定が落ちていた（CI で PR が赤になり run が `human` に落ちた）。両側に同じ凍った時計を配って決定的にした。`KNOWN_FLAKY` には足していない（自分のテストの書き方が原因のものを許容すると、本当の退行も rerun で流れるため）。ずれの検知・`timesyncd` の再起動・`[clock] guest offset ... → ...` の出力形式は変えていない。

### Changed
- **console: 左ナビを「チケット / 実行・監視 / 管理」の 3 群に分けた**。ボードと起票を隣り合わせ、AI Factory Manager・実行記録・ジョブ・ログ・統計を実行を見る群に、sandbox・鍵・設定を管理の群にまとめた（従来は 10 項目が頻度順で 1 列）。群の見出しは目印でリンクではない。選択中の項目には背景色に加えて `aria-current` が付く。行き先・件数・`g` + 頭文字の近道は変わらない（ADR-0089）

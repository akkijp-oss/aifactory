### Fixed
- **console: チケット本文と実行報告の Markdown で、裸の URL の切れ目と表を正しく表示する**。`http://localhost:3094、管理画面` のように日本語の句読点が続く行で、句読点から後ろまでリンク先に飲み込まれていた。あわせて Markdown の表（`| 見出し |` と `|---|`）が段落の文字列になっていたのを、見出しとセルを持つ表にし、狭い幅では横に流して読めるようにした。行内コードの中の URL はリンクにしない。

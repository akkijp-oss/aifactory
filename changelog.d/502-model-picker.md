### Changed
- **設定画面のモデルは「Agent + モデル名」の 2 段で選ぶ**。`claude-opus-5` のような ID を覚えたり写したりしなくても、
  表示名（`Opus 5` / `Sonnet 5` / `Fable 5.1` / `Haiku 4.5`）から選べる。今この設定で使っていないモデルにも替えられる。
  表に無い名前は今までどおり ID の欄に直接入力でき、既に入っている表に無い値も候補に残る。
  `routes.env` と workflow の定義に書かれる値はモデル ID 1 つのままで、保存の形式は変わらない（ADR-0072）。
